fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
sqlalchemy==2.0.35
httpx==0.27.2
tenacity==9.0.0
python-dotenv==1.0.1
numpy==1.26.4
Pillow==10.4.0
torch==2.4.1
loguru==0.7.2
python-multipart==0.0.9

# --- Streamlit analyst dashboard (presentation layer only — calls the
# same FastAPI backend above via HTTP; never duplicates its logic) ---
# Pinned to <1.58: streamlit>=1.58 hard-requires starlette>=0.40, which
# conflicts with fastapi==0.115.0's own pin of starlette<0.39,>=0.37.2.
# 1.55.0 is the newest release that coexists with the backend above in
# this SAME virtual environment with zero pip dependency conflicts
# (verified: `pip install --dry-run` against this exact file resolves
# cleanly) — see README > Streamlit dashboard for the full explanation.
streamlit==1.55.0
plotly>=5.24,<6

# testing
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0
respx==0.23.1
